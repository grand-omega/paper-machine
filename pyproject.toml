[project]
name = "new-paper-machine"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10.8",
    "numba>=0.65.0",
    "rich>=13.0",
    "yfinance>=1.3.0",
]

[tool.uv]
package = false
