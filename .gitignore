# Runtime state
state/
.agent_state/
agent_results/
events.jsonl
logs/
*.log

# Market-data cache (refetched via yfinance on first run of a fresh project)
data/

# Run archives — kept locally; user adds specific ones to git manually if they
# want to preserve a particular run's artifacts in history.
archive/

# Python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store

# Claude Code personal overrides
.claude/settings.local.json

# LaTeX build artifacts (regenerable by compile-latex skill)
paper/main.pdf
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.out
*.synctex.gz
*.toc
